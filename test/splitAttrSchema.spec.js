/* global splitAttrSchema */
'use strict';

/*describe('splitAttrSchema', function() {

  describe('for service', function() {
    it('"schema"', function() {
      expect(splitAttrSchema).toBeDefined();
      expect(splitAttrSchema('orderSchema')).toEqual({name:'orderSchema', prefix:undefined});
      expect(splitAttrSchema('orderSchema:order')).toEqual({name:'orderSchema', prefix:'order'});
      expect(splitAttrSchema('orderSchema:order.xxx')).toEqual({name:'orderSchema', prefix:'order.xxx'});
      expect(splitAttrSchema('orderSchema:order:xxx')).toEqual({name:'orderSchema', prefix:'order'});

      //expect(schema).toEqual(jasmine.any(Schema));
    });
  });

});*/

